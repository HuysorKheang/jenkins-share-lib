Quick notes:
- Add this repository as a Jenkins "Global Pipeline Library" with name: my-shared-lib
- Configure credentials and env vars: SONAR_HOST, SONAR_TOKEN, TELEGRAM_WEBHOOK, and Docker registry access
- Jenkinsfile demonstrates using the library functions and Dockerfiles stored under resources/dockerfiles
